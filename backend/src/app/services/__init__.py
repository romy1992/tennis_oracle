"""Application services (canonical domain layer for HTTP, bot, scheduler).

Prefer new business logic here. Tennis API import *implementations* still live
under ``backend.src.service`` and are orchestrated from ``imports`` /
``global_update``.
"""
