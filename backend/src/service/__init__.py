"""Legacy tennis-API import / sync helpers (active, not canonical).

Used by ``app.services.imports``, ``global_update``, and ``jobs.daily_pipeline``.
Do not add new domain features here — prefer ``app.services``. See
``docs/ARCHITECTURE_LAYERS.md``.
"""
