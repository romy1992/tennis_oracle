# Development / test dependencies.
# Install from repo root or backend/:
#   pip install -r backend/requirements-dev.txt
#
# Includes runtime deps via -r requirements.txt.

-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0